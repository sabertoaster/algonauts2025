[build-system]
requires = ["setuptools>=64", "setuptools_scm[toml]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "algonauts2025"
dynamic = ["version"]
description = "Algonauts 2025 modeling"
readme = "README.md"
requires-python = ">=3.11,<3.12"
dependencies = [
    "datasets",
    "huggingface_hub",
    "ipython",
    "jupyter",
    "nilearn",
    "matplotlib",
    "numpy<2",
    "omegaconf",
    "pandas",
    "pillow",
    "pre-commit",
    "pycortex",
    "pytest",
    "scikit-learn",
    "scipy",
    "timm",
    "torch==2.7.1",
    "tqdm",
    "transformers",
    "wandb",
    "yaspy",
    "neuromaps",
    "decord",
    "opencv-python",
    "einops",
    "torchaudio",
    "soundfile",
    "flash-attn @ https://github.com/mjun0812/flash-attention-prebuild-wheels/releases/download/v0.7.16/flash_attn-2.8.3+cu128torch2.7-cp311-cp311-linux_x86_64.whl",
]


[tool.setuptools_scm]
version_file = "src/_version.py"

[tool.uv.sources]
yaspy = { git = "https://github.com/childmindresearch/yaspy.git", rev = "629df8fe51fc085564b56ef2a2adddec869bfb9a" }